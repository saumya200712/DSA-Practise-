struct TreeNode {
    int val;
    TreeNode *left;
    TreeNode *right;

    TreeNode(int x) {
        val = x;
        left = NULL;
        right = NULL;
    }
};

Manual Represnation 
Node* root = new Node(1);
root->left = new Node(2);
root->right = new Node(3);

        1
       / \
      2   3
