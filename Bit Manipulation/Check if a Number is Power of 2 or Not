👉 Every power of 2 has exactly one set bit (1).
For powers of 2:
1000
0111
----
0000

The only set bit disappears, leaving 0.
That's why the trick works.

bool isPowerOfTwo(int n)
{
    return n > 0 && (n & (n - 1)) == 0;
}
