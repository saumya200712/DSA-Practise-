int singleNumber(vector<int>& nums) {

    int answer = 0;

    for(int number : nums)
    {
        answer ^= number;
    }

    return answer;
}

dry run
So:

4 ^ 1 ^ 2 ^ 1 ^ 2

Rearrange mentally:

4 ^ (1^1) ^ (2^2)
= 4 ^ 0 ^ 0
= 4

All duplicates cancel out!
