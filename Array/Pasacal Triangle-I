Element in Pascal Triangle
↓
(r-1 ) C (c-1)  (comibation and permuattaion wala kinda)
↓
Use nCr

class Solution {
public:
    int pascalTriangleI(int r, int c) {

        int n = r - 1;
        int requiredColoumn = c - 1;

//This give nCr formula

        long long ans = 1;

        for(int i=0; i<rr; i++)
        {
            ans = ans * (n - i);
            ans = ans / (i + 1);
        }

        return ans;
    }
};
